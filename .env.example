# Copy this to .env and fill in your values
# NEVER commit .env to GitHub

# Required in production — generate a random string
JWT_SECRET=change-this-to-a-long-random-string-in-production

# Optional: custom database path (Railway sets PORT automatically)
# DB_PATH=./data/game.db

# PORT is set automatically by Railway/Render — don't set this manually
